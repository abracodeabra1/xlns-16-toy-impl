diff --git a/ggml/CMakeLists.txt b/ggml/CMakeLists.txt
index c780077ac..f4f1b0bd9 100644
--- a/ggml/CMakeLists.txt
+++ b/ggml/CMakeLists.txt
@@ -188,6 +188,7 @@ option(GGML_SCHED_NO_REALLOC                "ggml: disallow reallocations in ggm
 # 3rd party libs / backends
 option(GGML_ACCELERATE                      "ggml: enable Accelerate framework"               ON)
 option(GGML_BLAS                            "ggml: use BLAS"                                  ${GGML_BLAS_DEFAULT})
+option(GGML_LNS                             "ggml: use LNS (Logarithmic Number System)"       OFF)
 set(GGML_BLAS_VENDOR ${GGML_BLAS_VENDOR_DEFAULT} CACHE STRING
                                             "ggml: BLAS library vendor")
 option(GGML_LLAMAFILE                       "ggml: use LLAMAFILE"                             ${GGML_LLAMAFILE_DEFAULT})
diff --git a/ggml/include/ggml.h b/ggml/include/ggml.h
index 669f66b65..3f3d25864 100644
--- a/ggml/include/ggml.h
+++ b/ggml/include/ggml.h
@@ -428,7 +428,8 @@ extern "C" {
         // GGML_TYPE_IQ4_NL_8_8 = 38,
         GGML_TYPE_MXFP4   = 39, // MXFP4 (1 block)
         GGML_TYPE_NVFP4   = 40, // NVFP4 (4 blocks, E4M3 scale)
-        GGML_TYPE_COUNT   = 41,
+        GGML_TYPE_LNS16   = 41, // 16-bit Logarithmic Number System (xlns16)
+        GGML_TYPE_COUNT   = 42,
     };
 
     // precision
diff --git a/ggml/src/CMakeLists.txt b/ggml/src/CMakeLists.txt
index 78853304d..da0fad608 100644
--- a/ggml/src/CMakeLists.txt
+++ b/ggml/src/CMakeLists.txt
@@ -446,6 +446,7 @@ elseif (GGML_CPU)
 endif()
 
 ggml_add_backend(BLAS)
+ggml_add_backend(LNS)
 ggml_add_backend(CANN)
 ggml_add_backend(CUDA)
 ggml_add_backend(HIP)
diff --git a/ggml/src/ggml-backend-reg.cpp b/ggml/src/ggml-backend-reg.cpp
index 058710921..f239a4721 100644
--- a/ggml/src/ggml-backend-reg.cpp
+++ b/ggml/src/ggml-backend-reg.cpp
@@ -66,6 +66,10 @@
 #include "ggml-blas.h"
 #endif
 
+#ifdef GGML_USE_LNS
+#include "ggml-lns.h"
+#endif
+
 #ifdef GGML_USE_RPC
 #include "ggml-rpc.h"
 #endif
@@ -155,6 +159,9 @@ struct ggml_backend_registry {
 #ifdef GGML_USE_BLAS
         register_backend(ggml_backend_blas_reg());
 #endif
+#ifdef GGML_USE_LNS
+        register_backend(ggml_backend_lns_reg());
+#endif
 #ifdef GGML_USE_RPC
         register_backend(ggml_backend_rpc_reg());
 #endif
diff --git a/ggml/src/ggml.c b/ggml/src/ggml.c
index 4c0764a0a..3b9c03394 100644
--- a/ggml/src/ggml.c
+++ b/ggml/src/ggml.c
@@ -726,6 +726,12 @@ static const struct ggml_type_traits type_traits[GGML_TYPE_COUNT] = {
         .to_float                 = (ggml_to_float_t) dequantize_row_nvfp4,
         .from_float_ref           = (ggml_from_float_t)quantize_row_nvfp4_ref,
     },
+    [GGML_TYPE_LNS16] = {
+        .type_name                = "lns16",
+        .blck_size                = 1,
+        .type_size                = sizeof(uint16_t),
+        .is_quantized             = false,
+    },
     [GGML_TYPE_Q2_K] = {
         .type_name                = "q2_K",
         .blck_size                = QK_K,
