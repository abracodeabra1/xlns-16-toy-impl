diff --git a/CMakeLists.txt b/CMakeLists.txt
index c780077a..f4f1b0bd 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -188,6 +188,7 @@ option(GGML_SCHED_NO_REALLOC                "ggml: disallow reallocations in ggm
 # 3rd party libs / backends
 option(GGML_ACCELERATE                      "ggml: enable Accelerate framework"               ON)
 option(GGML_BLAS                            "ggml: use BLAS"                                  ${GGML_BLAS_DEFAULT})
+option(GGML_LNS                             "ggml: use LNS (Logarithmic Number System)"       OFF)
 set(GGML_BLAS_VENDOR ${GGML_BLAS_VENDOR_DEFAULT} CACHE STRING
                                             "ggml: BLAS library vendor")
 option(GGML_LLAMAFILE                       "ggml: use LLAMAFILE"                             ${GGML_LLAMAFILE_DEFAULT})
diff --git a/include/ggml.h b/include/ggml.h
index 669f66b6..3f3d2586 100644
--- a/include/ggml.h
+++ b/include/ggml.h
@@ -428,7 +428,8 @@ extern "C" {
         // GGML_TYPE_IQ4_NL_8_8 = 38,
         GGML_TYPE_MXFP4   = 39, // MXFP4 (1 block)
         GGML_TYPE_NVFP4   = 40, // NVFP4 (4 blocks, E4M3 scale)
-        GGML_TYPE_COUNT   = 41,
+        GGML_TYPE_LNS16   = 41, // 16-bit Logarithmic Number System (xlns16)
+        GGML_TYPE_COUNT   = 42,
     };
 
     // precision
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 78853304..da0fad60 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -446,6 +446,7 @@ elseif (GGML_CPU)
 endif()
 
 ggml_add_backend(BLAS)
+ggml_add_backend(LNS)
 ggml_add_backend(CANN)
 ggml_add_backend(CUDA)
 ggml_add_backend(HIP)
diff --git a/src/ggml-backend-reg.cpp b/src/ggml-backend-reg.cpp
index 05871092..f239a472 100644
--- a/src/ggml-backend-reg.cpp
+++ b/src/ggml-backend-reg.cpp
@@ -66,6 +66,10 @@
 #include "ggml-blas.h"
 #endif
 
+#ifdef GGML_USE_LNS
+#include "ggml-lns.h"
+#endif
+
 #ifdef GGML_USE_RPC
 #include "ggml-rpc.h"
 #endif
@@ -155,6 +159,9 @@ struct ggml_backend_registry {
 #ifdef GGML_USE_BLAS
         register_backend(ggml_backend_blas_reg());
 #endif
+#ifdef GGML_USE_LNS
+        register_backend(ggml_backend_lns_reg());
+#endif
 #ifdef GGML_USE_RPC
         register_backend(ggml_backend_rpc_reg());
 #endif
diff --git a/src/ggml.c b/src/ggml.c
index 4c0764a0..fca184bd 100644
--- a/src/ggml.c
+++ b/src/ggml.c
@@ -14,6 +14,7 @@
 #include <hbwmalloc.h>
 #endif
 
+
 #if defined(_MSC_VER) || defined(__MINGW32__)
 #include <malloc.h> // using malloc.h with MSC/MINGW
 #elif !defined(__FreeBSD__) && !defined(__NetBSD__) && !defined(__OpenBSD__)
@@ -606,7 +607,7 @@ FILE * ggml_fopen(const char * fname, const char * mode) {
 
 }
 
-static const struct ggml_type_traits type_traits[GGML_TYPE_COUNT] = {
+static struct ggml_type_traits type_traits[GGML_TYPE_COUNT] = {
     [GGML_TYPE_I8] = {
         .type_name                = "i8",
         .blck_size                = 1,
@@ -726,6 +727,14 @@ static const struct ggml_type_traits type_traits[GGML_TYPE_COUNT] = {
         .to_float                 = (ggml_to_float_t) dequantize_row_nvfp4,
         .from_float_ref           = (ggml_from_float_t)quantize_row_nvfp4_ref,
     },
+    [GGML_TYPE_LNS16] = {
+        .type_name                = "lns16",
+        .blck_size                = 1,
+        .type_size                = sizeof(uint16_t),
+        .is_quantized             = false,
+        // to_float / from_float_ref are registered at runtime by ggml_backend_lns_reg()
+        // to avoid a circular build dependency between ggml-base and ggml-lns.
+    },
     [GGML_TYPE_Q2_K] = {
         .type_name                = "q2_K",
         .blck_size                = QK_K,
